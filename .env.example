# Server-only. Keep the real key in .env.local or the deployment's secrets.
RESEND_API_KEY=
# Testing: use onboarding@resend.dev and your Resend account email as TO_EMAIL.
# Production: use an address on a domain verified in your Resend account.
FROM_EMAIL=Portfolio <onboarding@resend.dev>
TO_EMAIL=
